# 🚀 快速启动指南

## 一键启动（推荐）

```bash
npm run dev
```

这将**同时启动前后端**：
- ✅ 前端开发服务器：`http://localhost:3000`
- ✅ 后端API服务：`http://localhost:8000`

## 首次使用

### 1. 安装依赖

```bash
# 安装前端依赖（根目录）
npm install

# 安装后端依赖
cd server
npm install
cd ..
```

### 2. 配置API密钥

```bash
# 复制环境变量模板
cp server/.env.example server/.env

# 编辑 server/.env，填入你的ChatGLM API密钥
# CHATGLM_API_KEY=your_api_key_here
```

### 3. 启动项目

```bash
npm run dev
```

## 可用命令

### 统一启动（推荐）
- `npm run dev` - 同时启动前后端开发服务器
- `npm start` - 同时启动前后端生产服务器
- `npm run build` - 构建前后端

### 单独启动
- `npm run dev:frontend` - 仅启动前端
- `npm run dev:backend` - 仅启动后端
- `npm run build:frontend` - 仅构建前端
- `npm run build:backend` - 仅构建后端

## 验证

启动后访问：
- 🌐 前端：http://localhost:3000
- 🔧 后端健康检查：http://localhost:8000/api/health
- 📋 后端API信息：http://localhost:8000/

## 项目结构

```
.
├── server/              # 后端服务 (TypeScript/Express)
│   ├── src/            # 源代码
│   ├── dist/           # 编译输出
│   └── .env            # 环境变量（需要创建）
├── components/         # React组件
├── services/           # 前端服务
└── package.json        # 前端依赖和统一启动脚本
```

## 故障排查

### 端口被占用
如果8000或3000端口被占用：
1. 停止占用端口的进程
2. 或修改 `server/.env` 中的 `PORT` 配置

### API调用失败
1. 检查 `server/.env` 中的API密钥是否正确
2. 访问 `http://localhost:8000/api/health` 验证服务状态
3. 查看浏览器控制台和服务器日志
